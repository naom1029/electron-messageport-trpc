{
  "name": "electron-messageport-trpc-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter electron-messageport-trpc build",
    "test": "pnpm -r --stream test",
    "test:ci": "pnpm -r --stream test:ci",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "knip": "knip",
    "release": "pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "knip": "^6.16.0",
    "typescript": "^6.0.3"
  }
}
