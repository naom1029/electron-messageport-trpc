{
  "name": "electron-messageport-trpc-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --stream test",
    "test:ci": "pnpm -r --stream test:ci",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "release": "pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "typescript": "^5.9.0"
  }
}
