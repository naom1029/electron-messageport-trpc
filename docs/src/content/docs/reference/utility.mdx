---
title: Utility Process API
description: API reference for electron-messageport-trpc/utility
---

```typescript
import {
  createElectronTRPCUtility,
  createParentPortHandler,
} from 'electron-messageport-trpc/utility';
```

## `createElectronTRPCUtility(opts)`

Connects a utility process router to a typed registry channel.

```typescript
import { createElectronTRPCUtility } from 'electron-messageport-trpc/utility';
import { electronTRPC } from './trpc';
import { workerRouter } from './router';

createElectronTRPCUtility({
  channel: electronTRPC.worker,
  router: workerRouter,
  parentPort: process.parentPort,
});
```

The `router` must match the router type registered for the selected channel, and
the handler only serves the selected channel.

### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `channel` | `ElectronTRPCChannel` | Yes | The registry channel this utility serves (e.g. `electronTRPC.worker`) |
| `router` | `RouterForChannel<TChannel>` | Yes | The tRPC router for that channel |
| `parentPort` | `ParentPortLike` | Yes | The utility process parent port (`process.parentPort`) |
| `createContext` | `() => MaybePromise<unknown>` | No | Per-request context factory |
| `transformer` | `DataTransformerOptions` | No | Overrides the router's configured data transformer; must match the client end |

### Returns

A `ParentPortHandler` with `handlers` and `destroy()`.

---

## `createParentPortHandler(opts)`

Attaches a tRPC request handler to the utility process's `parentPort`. This allows the main process (or, via brokering, a renderer) to call tRPC procedures running in a utility process.

### Parameters

```typescript
interface CreateParentPortHandlerOptions<TRouter extends AnyRouter> {
  router: TRouter;
  parentPort: ParentPortLike;
  channel?: string;
  createContext?: () => MaybePromise<unknown>;
  transformer?: DataTransformerOptions;
}
```

| Parameter | Type | Required | Description |
|---|---|---|---|
| `router` | `AnyRouter` | Yes | Your tRPC router instance |
| `parentPort` | `ParentPortLike` | Yes | The utility process parent port. `ParentPortLike` is a structural type (not Electron's `ParentPort`); `process.parentPort` is assignable to it |
| `channel` | `string` | No | Only handle ports sent for this channel |
| `createContext` | `() => MaybePromise<unknown>` | No | Factory function called for every request. May return synchronously or return a promise |
| `transformer` | `DataTransformerOptions` | No | Overrides the router's configured data transformer |

### Returns

A `ParentPortHandler` object with `handlers` and `destroy()`. Each transferred
port creates one child `PortHandler`.

### Example

```typescript
// utility-worker.ts
import { createParentPortHandler } from 'electron-messageport-trpc/utility';
import { initTRPC } from '@trpc/server';

const t = initTRPC.create();

const router = t.router({
  heavyComputation: t.procedure.query(async () => {
    // Runs in a separate thread, not blocking the main process
    return performExpensiveWork();
  }),
});

createParentPortHandler({
  router,
  parentPort: process.parentPort,
});
```

```typescript
// main process
import { MessageChannelMain, utilityProcess } from 'electron';
import { createTRPCClient } from '@trpc/client';
import { mainPortLink } from 'electron-messageport-trpc/main';

const child = utilityProcess.fork('utility-worker.js');
const { port1, port2 } = new MessageChannelMain();

child.postMessage({ type: 'connect' }, [port1]);

const client = createTRPCClient({
  links: [mainPortLink({ port: port2 })],
});
```

### Use Cases

- **CPU-intensive computation** without blocking the main process
- **File processing** in a background thread
- **Database operations** isolated from the main process
- **Renderer-to-utility brokering** when the main process should only hand off a port
